#! /bin/bash
set -eux

num2str() {
  octal=$(printf '%03o' "$1")
  printf "\\$octal"
}

inputs=(3 7)

actual_str=$(
  for input in "${inputs[@]}"; do
    num2str "$input"
    echo
  done |
  brainfuck mul.bf
)

actual_num=$(printf '%d' "'$actual_str")

test "$actual_num" = 21
